function isHasLetter(letter, str) {
    return str.indexOf(letter) !== -1;

    // if (str.indexOf(letter) !== -1) {
    //     return true;
    // }
    // return false;


}
function isUpFromNumber(min, number) {
    return number > min;

}